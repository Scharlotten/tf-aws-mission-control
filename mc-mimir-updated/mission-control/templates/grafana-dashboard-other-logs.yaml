{{- if .Values.grafana.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8ssandra-common.fullname" . }}-other-logs-dashboard
  labels: {{ include "k8ssandra-common.labels" . | indent 4 }}
    grafana_dashboard: "1"
data:
  loki-others.json: |
{{ .Files.Get "dashboards/loki-others.json" | indent 4 }}
{{- end }}